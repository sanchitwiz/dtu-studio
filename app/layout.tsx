import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import "./globals.css"
import { Providers } from "../components/providers" // ✅ Import client wrapper

export const metadata: Metadata = {
  title: "Photography & Videography Scheduling",
  description: "College Photography & Videography team scheduling system",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body className="antialiased">
        <Providers> {/* ✅ Client component wrapper */}
          {children}
        </Providers>
      </body>
    </html>
  )
}
